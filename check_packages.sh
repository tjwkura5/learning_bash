# Write a script that loops through a list of software packages 
# (systemd, htop, git, python3, curl, nmap, apt) and 
# checks their current version on the system. 
# If a package is not installed, ask to install the package. 
# Otherwise, print the installed version.